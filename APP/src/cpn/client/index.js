import Home from './splitter';

import { SuUsers, SuUser, EditUser } from './users';
import { Version, TableInput, APIPostInput, DataClient, APIPutInput } from './version';

export{
    Home,  SuUsers, SuUser, EditUser, Version, TableInput, APIPostInput, DataClient, APIPutInput}
